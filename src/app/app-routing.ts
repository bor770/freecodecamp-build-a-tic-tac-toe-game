import { Routes } from '@angular/router';

import { NumberOfPlayersComponent } from './number-of-players/number-of-players.component';

export const routes: Routes = [
  { component: NumberOfPlayersComponent, path: `` },
];
